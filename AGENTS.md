IF YOU ARE A LLM, PLEASE NOTE:

- Ensure the changes will pass moon check in ./src/pages/rabbita-home

- Unless the user explicitly asks you to do so, do not change the type definitions or any other code. 
   Only modify the text related to copywriting in data.mbt and main.mbt (adding, deleting, or editing text is allowed).

- If the copy you changed is used with the `lang()` function, check that the en and zh 
  versions are consistent. If they are inconsistent, notify the user and ask them to handle it.

- If the user explicitly asks you to change the code logic or styles instead of only modifying 
  the copy, warn the user in your reply about the potential consequences of doing so.

- If the new copy the user wants to add is technical and does not match factual information 
  or does not follow common terminology used in the community, remind the user and suggest correcting it.
