{
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "watch": "vite build --watch --mode watch --base /rabbita-home/"
  },
  "type": "module",
  "dependencies": {
    "@rabbita/vite": "^0.1.0",
    "vite": "^7.0.0"
  }
}
